# 🧠 GPT 프롬프트 템플릿

## 📋 시스템 프롬프트
```
너는 YAGO VIBE 음성 지도 시스템의 일일 리포트를 작성하는 AI 비서야. 
사용자의 음성 명령 데이터를 분석하고 전문적이고 읽기 쉬운 한국어 리포트를 작성해줘.

리포트 작성 규칙:
1. 친근하고 전문적인 톤 사용
2. 데이터 기반 인사이트 제공
3. 사용자 행동 패턴 분석
4. 개선 제안 포함
5. 이모지와 함께 가독성 높이기
```

## 📊 사용자 프롬프트 템플릿
```
다음은 YAGO VIBE 시스템의 일일 음성 명령 데이터입니다:

📅 날짜: {{$json.date}}
🎙️ 총 명령: {{$json.total}}건
✅ 성공률: {{$json.successRate}}

📈 Intent별 통계:
{{Object.entries($json.intents).map(([intent, count]) => `- ${intent}: ${count}건`).join('\n')}}

🔥 상위 키워드 Top 5:
{{$json.topKeywords.map(k => `- ${k.keyword}: ${k.count}회`).join('\n')}}

⏰ 가장 활발한 시간: {{$json.peakHour}}
📅 가장 활발한 요일: {{$json.peakDay}}

위 데이터를 바탕으로 다음 형식으로 리포트를 작성해주세요:

1. 📊 일일 요약
2. 🎯 주요 인사이트
3. 📈 사용 패턴 분석
4. 💡 개선 제안
5. 🚀 내일의 목표
```

## 🎨 리포트 출력 형식
```
📊 **YAGO VIBE 일일 리포트** - {{$json.date}}

## 📈 일일 요약
오늘 총 {{$json.total}}건의 음성 명령이 처리되었으며, 성공률은 {{$json.successRate}}입니다.

## 🎯 주요 인사이트
- 가장 많이 사용된 기능: {{가장 높은 intent}}
- 사용자 선호 시간대: {{$json.peakHour}}
- 인기 검색 키워드: {{$json.topKeywords[0].keyword}}

## 📊 사용 패턴 분석
{{시간대별, 요일별 패턴 분석}}

## 💡 개선 제안
{{데이터 기반 개선 제안}}

## 🚀 내일의 목표
{{다음날 목표 설정}}
```

## 🔧 n8n에서 사용 방법

### 1️⃣ OpenAI Node 설정
```json
{
  "parameters": {
    "model": "gpt-4o-mini",
    "messages": [
      {
        "role": "system",
        "content": "너는 YAGO VIBE 음성 지도 시스템의 일일 리포트를 작성하는 AI 비서야..."
      },
      {
        "role": "user",
        "content": "다음은 YAGO VIBE 시스템의 일일 음성 명령 데이터입니다: {{$json}}"
      }
    ]
  }
}
```

### 2️⃣ 응답 처리
```javascript
// GPT 응답을 HTML로 변환
const gptResponse = items[0].json.choices[0].message.content;
const htmlContent = `
<html>
<head>
  <style>
    body { font-family: 'Malgun Gothic', sans-serif; margin: 20px; }
    h1 { color: #1a73e8; }
    h2 { color: #34a853; }
    .summary { background: #f8f9fa; padding: 15px; border-radius: 8px; }
  </style>
</head>
<body>
  <h1>📊 YAGO VIBE 일일 리포트</h1>
  <div class="summary">
    ${gptResponse.replace(/\n/g, '<br>')}
  </div>
</body>
</html>
`;

return [{ json: { html: htmlContent, summary: gptResponse } }];
```
