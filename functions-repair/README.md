# functions-repair (deprecated)

`repairTeamMembersSoTFromIndex` 구현은 **`functions/src/team/repairTeamMembersSoTCallable.ts`** 로 옮겼고, `firebase.json`의 별도 repair 코드베이스 항목은 제거되었습니다.

배포: 루트에서 `firebase deploy --only functions` 또는 `npm run deploy:functions:repair`(기본 `functions:default` 배포).
