# 🔥 Google 로그인 "The requested action is invalid" 최종 해결

## 🚨 현재 상황
- 오류: "The requested action is invalid"
- URL: `https://yago-vibe-spt.firebaseapp.com/_/auth/handler?apiKey=...`
- 콘솔에 특별한 오류 없음

## ✅ 반드시 확인해야 할 3가지

### 1. Google Cloud Console > OAuth 클라이언트 > 리디렉션 URI (가장 중요!)

**문제**: URL에 `_/auth/handler` (언더스코어 1개)가 보이지만, Firebase는 `__/auth/handler` (언더스코어 2개)를 사용합니다.

#### 확인 방법:

1. **Google Cloud Console 접속**
   - https://console.cloud.google.com
   - 프로젝트 "yago-vibe-spt" 선택

2. **OAuth 클라이언트로 이동**
   - 왼쪽 메뉴 > "API 및 서비스" > "사용자 인증 정보"
   - "OAuth 2.0 클라이언트 ID" 섹션에서 클라이언트 찾기
   - 클라이언트 클릭 (편집)

3. **승인된 리디렉션 URI 확인**
   - "승인된 리디렉션 URI" 섹션 확인
   - 다음이 **정확히** 포함되어 있는지 확인 (언더스코어 **2개**):
     - `http://localhost:5173/__/auth/handler` ✅ (언더스코어 2개)
     - `http://localhost:5174/__/auth/handler` ✅ (언더스코어 2개)
     - `https://yago-vibe-spt.firebaseapp.com/__/auth/handler` ✅ (언더스코어 2개)
     - `https://yago-vibe-spt.web.app/__/auth/handler` ✅ (언더스코어 2개)

4. **잘못된 URI 제거**
   - `_/auth/handler` (언더스코어 1개)가 있으면 **삭제**
   - `//auth/handler` (슬래시 2개)가 있으면 **삭제**

5. **올바른 URI 추가 (없는 경우)**
   - "+ URI 추가" 클릭
   - `http://localhost:5173/__/auth/handler` 입력 (언더스코어 2개!)
   - "저장" 클릭

### 2. OAuth 동의 화면 > 테스트 사용자 추가

1. **Google Cloud Console > OAuth 동의 화면**
   - 왼쪽 메뉴 > "API 및 서비스" > "OAuth 동의 화면"

2. **테스트 사용자 확인**
   - "테스트 사용자" 섹션 확인
   - `jaeman2034@gmail.com`이 목록에 있는지 확인
   - 없으면:
     - "+ 사용자 추가" 클릭
     - 이메일 입력: `jaeman2034@gmail.com`
     - "저장" 클릭

3. **사용자 유형 확인**
   - "외부" 선택되어 있으면 테스트 사용자 필수!
   - "내부" 선택되어 있으면 테스트 사용자 불필요

### 3. Identity Toolkit API 활성화

1. **Google Cloud Console > API 라이브러리**
   - 왼쪽 메뉴 > "API 및 서비스" > "라이브러리"

2. **Identity Toolkit API 검색**
   - 검색창에 "Identity Toolkit API" 입력
   - 클릭

3. **활성화 확인**
   - "사용 설정됨" 또는 "관리" 버튼이 보이면 활성화됨 ✅
   - "사용 설정" 버튼이 보이면 비활성화 → 클릭하여 활성화

## 🔥 즉시 해야 할 것

### Step 1: 리디렉션 URI 수정 (가장 중요!)

1. Google Cloud Console > OAuth 클라이언트로 이동
2. "승인된 리디렉션 URI" 섹션 확인
3. 모든 URI가 `__/auth/handler` (언더스코어 2개)로 되어 있는지 확인
4. `_/auth/handler` (언더스코어 1개)가 있으면 삭제
5. 올바른 URI 추가:
   - `http://localhost:5173/__/auth/handler`
   - `https://yago-vibe-spt.firebaseapp.com/__/auth/handler`
6. "저장" 클릭

### Step 2: 테스트 사용자 추가

1. Google Cloud Console > OAuth 동의 화면
2. 테스트 사용자에 `jaeman2034@gmail.com` 추가
3. "저장" 클릭

### Step 3: 브라우저 완전 초기화

1. **모든 Chrome 창 닫기**
2. **시크릿 모드에서 새로 열기** (Ctrl + Shift + N)
3. **`http://localhost:5173/login` 접속**
4. **Google 로그인 테스트**

### Step 4: 2-3분 대기

- 설정 변경 후 Google Cloud Console에서 변경사항이 반영되는데 시간이 걸립니다
- 2-3분 대기 후 다시 테스트

## 📋 최종 체크리스트

- [ ] Google Cloud Console > OAuth 클라이언트 > 승인된 리디렉션 URI에 `http://localhost:5173/__/auth/handler` 포함 (언더스코어 2개)
- [ ] Google Cloud Console > OAuth 클라이언트 > `_/auth/handler` (언더스코어 1개) URI 삭제됨
- [ ] Google Cloud Console > OAuth 동의 화면 > 테스트 사용자에 `jaeman2034@gmail.com` 추가됨
- [ ] Google Cloud Console > API 라이브러리 > Identity Toolkit API 활성화됨
- [ ] 브라우저 완전 초기화 (시크릿 모드)
- [ ] 2-3분 대기 후 테스트

## 💡 핵심 포인트

**"The requested action is invalid" 오류의 가장 흔한 원인:**

1. **리디렉션 URI 불일치** (80% 확률)
   - `_/auth/handler` (언더스코어 1개) → 잘못됨 ❌
   - `__/auth/handler` (언더스코어 2개) → 올바름 ✅

2. **테스트 사용자 미추가** (15% 확률)
   - 외부 사용자 유형일 때 테스트 사용자 필수

3. **Identity Toolkit API 미활성화** (5% 확률)

## 🚨 여전히 안 되면

브라우저 콘솔에서 다음을 확인하세요:

1. F12 > Console 탭
2. Google 로그인 버튼 클릭
3. 나타나는 모든 메시지 확인 (빨간색 오류 포함)
4. 오류 메시지를 알려주세요

