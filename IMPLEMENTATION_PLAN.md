# 구현 계획: 1→2→3 순서대로 진행

## 현재 상태 확인

### 이미 존재하는 것들
- ✅ `NoticeListPage` - `/association/:id/notices` (라우트 존재 확인 필요)
- ✅ `TournamentListPage` - `/association/:id/tournaments` (라우트 존재 확인 필요)
- ✅ `FacilityListPage` - `/association/:id/facility` (라우트 존재 확인 필요)
- ✅ `SectionLayout` 컴포넌트 생성 완료

### 작업 필요
1. HeroSection에서 #notice, #tournament, #facility 버튼을 Link로 교체
2. 라우트 확인 및 추가 (이미 있을 수 있음)
3. 3페이지에 SectionLayout 적용
4. OfficialBadge + OfficialFooter 통합

## 다음 진행

**"다음 진행"** - 단계별로 순서대로 진행:
1. HeroSection 확인 → 버튼을 Link로 교체
2. 라우트 확인 → 없으면 추가
3. 3페이지에 SectionLayout 적용
4. OfficialBadge + OfficialFooter 통합

준비 완료. 구현 시작할까요?

