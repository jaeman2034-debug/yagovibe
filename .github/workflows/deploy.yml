name: 🔥 Firebase Auto Deploy

on:
  push:
    branches:
      - main
    paths-ignore:
      - '**/*.md'
      - '**/*.txt'
      - '.gitignore'

jobs:
  build-and-deploy:
    name: Build & Deploy to Firebase
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      # 1️⃣ 저장소 체크아웃
      - name: Checkout Repository
        uses: actions/checkout@v4

      # 2️⃣ Node.js 환경 설정
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      # 3️⃣ 의존성 설치
      - name: Install Dependencies
        run: npm ci

      # 4️⃣ 프로젝트 빌드
      - name: Build Project
        run: npm run build
        env:
          VITE_OPENAI_API_KEY: ${{ secrets.VITE_OPENAI_API_KEY }}
          VITE_KAKAO_API_KEY: ${{ secrets.VITE_KAKAO_API_KEY }}
          VITE_SLACK_WEBHOOK_URL: ${{ secrets.VITE_SLACK_WEBHOOK_URL }}

      # 5️⃣ Firebase 배포
      - name: Deploy to Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT }}'
          channelId: live
          projectId: yago-vibe-spt
          target: yago-vibe-spt

      # 6️⃣ 배포 완료 알림
      - name: Deploy Success Notification
        if: success()
        run: |
          echo "✅ 배포 완료!"
          echo "🌐 URL: https://yago-vibe-spt.web.app"
          echo "🌐 Custom: https://app.yagovibe.com"

      # 7️⃣ 배포 실패 알림
      - name: Deploy Failure Notification
        if: failure()
        run: |
          echo "❌ 배포 실패!"
          echo "로그를 확인하세요."

