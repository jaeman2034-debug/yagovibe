/** TRACK 9A Phase C-3 — Phaser registry keys (single source of truth) */

export const MATCH_REGISTRY_KEYS = {
  unifiedMatchConfig: "unifiedMatchConfig",
  liveMatchConfig: "liveMatchConfig",
  matchBehavior: "matchBehavior",
  matchSceneParity: "matchSceneParity",
  fieldLayoutMode: "fieldLayoutMode",
  liveMatchReady: "liveMatchReady",
} as const;
