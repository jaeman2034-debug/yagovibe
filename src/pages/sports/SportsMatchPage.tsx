import MatchListPage from "@/pages/match/MatchListPage";

/**
 * 참여 전용 화면 — /sports/match
 * 허브(/sports)와 분리해서 "지금 참여 가능한 경기" 흐름에 집중한다.
 */
export default function SportsMatchPage() {
  return <MatchListPage />;
}
