/**
 * 🔥 테스트 버전: MarketAddPage 최소 구현
 * 
 * 이 파일로 import가 정상 작동하는지 확인
 */

export default function MarketAddPage() {
  return (
    <div style={{ padding: 40 }}>
      <h1>MARKET CREATE PAGE OK</h1>
      <p>이 화면이 보이면 import는 정상입니다.</p>
    </div>
  );
}

