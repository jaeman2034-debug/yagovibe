/** Sprint E-2.4 — Coach Dashboard snapshot */

export type CoachDashboardSnapshot = {
  actionNeededCount: number;
  atRiskCount: number;
  recommendedTrainingCount: number;
  alertCount: number;
};
